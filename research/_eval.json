{"function":"() => { const get = (sel, p) => { const el = document.querySelector(sel); return el ? getComputedStyle(el)[p] : null; }; const body = getComputedStyle(document.body); const h1 = document.querySelector('.hero h1'); const lede = document.querySelector('.hero p.lede'); const eyebrow = document.querySelector('.hero .eyebrow'); const statB = document.querySelector('.stat b'); const secH2 = document.querySelector('.sec-head h2'); const pill = document.querySelector('.pill[aria-pressed=\"true\"]'); const card = document.querySelector('.card'); const cardH3 = document.querySelector('.card h3'); const sky = document.querySelector('.sky'); const navH = document.querySelector('.nav-in'); return { bodyFont: body.fontFamily, bodyBg: body.backgroundColor, bodyColor: body.color, h1Font: getComputedStyle(h1).fontSize + ' / ' + getComputedStyle(h1).fontWeight + ' / lh=' + getComputedStyle(h1).lineHeight + ' / ls=' + getComputedStyle(h1).letterSpacing, h1MaxWidth: getComputedStyle(h1).maxWidth, ledeFont: getComputedStyle(lede).fontSize + ' / ' + getComputedStyle(lede).lineHeight + ' / color=' + getComputedStyle(lede).color, eyebrowFont: getComputedStyle(eyebrow).fontSize + ' / ls=' + getComputedStyle(eyebrow).letterSpacing + ' / color=' + getComputedStyle(eyebrow).color, statFont: getComputedStyle(statB).fontSize + ' / w=' + getComputedStyle(statB).fontWeight, secH2Font: getComputedStyle(secH2).fontSize + ' / w=' + getComputedStyle(secH2).fontWeight, pillFont: getComputedStyle(pill).fontSize + ' / bg=' + getComputedStyle(pill).backgroundImage, cardBorderRadius: getComputedStyle(card).borderRadius, cardPadding: getComputedStyle(card.querySelector('.body')).padding, cardH3Font: getComputedStyle(cardH3).fontSize + ' / w=' + getComputedStyle(cardH3).fontWeight, skyZ: getComputedStyle(sky).zIndex, navHeight: getComputedStyle(navH).height, navBackdrop: getComputedStyle(navH).backdropFilter }; }"}